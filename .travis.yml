language: java

# Use Ubuntu 14.04 instead of Ubuntu 12.04
sudo: required
dist: trusty

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y binutils-dev

script: ./.travis-build.sh

jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7

git:
  depth: 3
